import React from "react";
import PropTypes from "prop-types";

const ChatSub = () => {
  return (
    <div
      style={{
        display: "flex",
        flex: 1,
        justifyContent: "center",
        margin: "10px"
      }}
    >
      <h1>Chat</h1>
    </div>
  );
};

ChatSub.propTypes = {};

export default ChatSub;
